@page
@model HospitalManagement.Pages.Views.Doctors.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Doctor[0].Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Doctor[0].Gender)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Doctor[0].Designation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Doctor[0].Address)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Doctor[0].Surgeon)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Doctor) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Gender)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Designation)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Address)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Surgeon)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.DrCode">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.DrCode">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.DrCode">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
